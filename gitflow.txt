# Setup inicial
git clone <url>
cd primer_parcial_ped
git checkout -b main
git checkout -b develop

# Por cada función nueva
git checkout develop
git checkout -b feature/nombre-funcion
# ... crear archivo y escribir código ...
git add .
git commit -m "feat: Descripcion clara del cambio"
git checkout develop
git merge feature/nombre-funcion

# Al final
git checkout main
git merge develop
git push origin main
git push origin develop