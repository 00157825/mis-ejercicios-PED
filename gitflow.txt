git clone <url-del-repo-primer_parcial_ped>
cd primer_parcial_ped
git checkout -b main
git checkout -b develop

//A PARTIR DE ACA SE REPITE EL PROCESO

git checkout develop
git checkout -b feature/struct-elemento

git add .
git commit -m "feat: Define struct Elemento con sus campos"

git checkout develop
git merge feature/struct-elemento

//HASTA ACÁ, HASTA TERMINAR TODAS LAS FUNCIONES

git checkout main
git merge develop
git push origin main
git push origin develop